import { ArrayView } from "./array-view.js";
export declare class TypedArrayView<T> extends ArrayView<T> {
    static offset: number;
    static getOffset(index: number): number;
    static getSize(length: number): number;
}
